# Healthcare Application Development Environment Configuration Template
# Copy this file to .env.development and customize the values

# Application Settings
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:3000

# Database Configuration (PostgreSQL) - Docker Development
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=healthapp_dev
POSTGRES_USER=healthapp_user
POSTGRES_PASSWORD=change_this_password_in_production
DB_DIALECT=postgres
DB_TIMEZONE=+00:00

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change_this_redis_password

# JWT Configuration
JWT_SECRET=change-this-to-a-secure-256-bit-secret-key-for-production
JWT_EXPIRES_IN=24h

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# AWS S3 Configuration (Optional for development)
# Uncomment and configure for file upload functionality
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=healthapp-dev-bucket

# Email Configuration (Optional for development)
# Uncomment and configure for email notifications
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASS=your_app_password

# Monitoring Configuration (Optional)
# GRAFANA_PASSWORD=admin123

# Development Specific Settings
DEBUG=healthapp:*
LOG_LEVEL=debug

# IMPORTANT SECURITY NOTES:
# 1. Change all passwords and secrets before deploying to production
# 2. Never commit real credentials to version control
# 3. Use environment-specific values for different deployments