# Development Environment Variables (Docker Swarm)
NODE_ENV=development
PORT=3005

# Database Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=healthapp_dev
POSTGRES_USER=healthapp_user
POSTGRES_PASSWORD=pg_password

# JWT Configuration  
JWT_SECRET=dev_jwt_secret_key_2024

# CORS Configuration (will be set by deployment script)
CORS_ORIGIN=http://localhost:3002

# Frontend Configuration (will be set by deployment script)
NEXT_PUBLIC_API_URL=http://localhost:3005/api
NEXT_PUBLIC_BACKEND_URL=http://localhost:3005

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# Multi-server deployment variables (set by script)
FRONTEND_IP=localhost
BACKEND_IP=localhost
POSTGRES_IP=localhost
REDIS_IP=localhost